server:
  port: 8081
spring:
  application:
    name: Eureka-Server
eureka:
  instance:
    hostname: peer2
  client:
    service-url:
      defaultZone:http://peer1.8080/eureka/
  server:
    enable-self-preservation: false